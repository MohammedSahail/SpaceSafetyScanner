# Safety Scanner

This repo contains a minimal Streamlit UI to preview and test detection UI.

## Run locally
1. python -m venv .venv
2. .venv/bin/activate  (Windows: .venv\Scripts\activate)
3. pip install -r requirements.txt
4. streamlit run app.py

## Deploy
Push this repo to GitHub and connect it on https://share.streamlit.io
